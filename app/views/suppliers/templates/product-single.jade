.row.product-single(tag=index)
    .three.columns
        - if (product.images[0] && product.images[0].files && product.images[0].files.length)
            img(src=product.images[0].cdnUri + '/mini_' + product.images[0].files[0], style="margin-top: -5px; height: 60px; width: 40px")
    .eight.columns
        p 
          span(style="width:100%")
            b=product.name 
            br
          span(style="width: 100%")
            b £
            =product.price